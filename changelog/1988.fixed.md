Fix batched `wp.optim.linear` solvers (`cg()`, `cr()`, `bicgstab()`, `gmres()`) stopping before the largest subproblem converges when `maxiter` is omitted and batches have uneven sizes. The default now covers the largest subproblem, using `max_batch_length` when provided and the total system size otherwise.
